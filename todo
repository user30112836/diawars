Bugs:
- PvP logic is reversed (Might be fixed)
-1 Player inputs can serialize into buttons (XSS)
    -> /bug report renders into /bug list (BugManager.kt:41)
    -> event names render into the event lists (EventManager.kt:281)
    -> Solution: escape before serializing (via smm = SafeMiniMessage)
-10 SendToLobby marks the player in-lobby before it can fail (LobbyManager.kt:48)
-15 Vault count changes on events that could be cancelled (VaultListener.kt)
-/ Players can place diamonds in bundles and surpass the limit
-/ Passively moved players don't get the AFK status

Improvements
-8 Improve sidebar layout (DiamondScoreboardManager.kt:143)
-14 PopulateMainMenu can abort mid-render (MainMenu.kt:41)
-! Instead of itemStack AIR there should be item.remove() (DiamondLimitManager.kt:200)
-! display.billboard = display.billboard is a no-op (DiamondLimitManager.kt:86)
-! Swap armor order in the inspection inventory (InventoryInspectManager.kt:135)

Performance:
-2 Many particles for the border
-3 Each lockup for spawn points takes a disk read

Events:
- Make things configurable when setting up events
    -> World type
    -> Player gamemode
    -> World daytime
    -> Gamerules: keep_inventory, advance_time, advance_weather, mob_griefing, mob_drops, spawn_...
    -> Rewards
    -> Effects
    -> Inventory
- Players should be able to edit rejected event worlds

Anti-Cheat:
-/ Create communication with a client-side mod

Manual:
- Default contents of the manual should be tuned

Multiplayer-Tests:
- Are personal configs getting deleted when offline?
- Is the PvPStatus working correctly?
- Are fights getting detected properly?
- Does the vault invite system work?