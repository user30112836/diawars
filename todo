Bugs:
- 1 PvP logic is reversed (Might be fixed)
- 2 Inventory in event worlds does not get saved
- 4 Border does not align correctly (It's fine on default settings)

Improvements:
=== Empty ===

Lobby:
- Make the player spawn there on first join
- When the player first leaves the lobby he should spawn in his zone
- When a player hasn't left the lobby yet they should receive a welcome message
    -> Recommend the player to increase his particles to see the border properly
    -> Short introduction to the plugin and a reference to the manual

Events:
- Players should not be able to join event worlds when they exceed the diamond limit
- 12 Make things configurable when setting up events
    -> World type
    -> Player gamemode
    -> World daytime
    -> Gamerules: advance_time, advance_weather, mob_griefing, mob_drops, spawn_...
    -> Rewards
    -> Effects
    -> Inventory

Features:
- Log every interaction with diamonds

Reports:
- Way for players to report bugs in the plugin via /bug
- Notifying ops when new bugs got reported
- Op can get a list of the bugs (with player names)
- Op can mark bugs as read or resolved
- When "unread" bugs are detected on join the op gets notified

Admin Command:
- 15 Inspecting inv of players

Manual:
- Organised in different segments
    -> Can be accessed through the menu with submenus
    -> Opens a temporary book (no item) with the selected segment
- Segments
    -> Full manual (every segment combined in one book)
    -> Quick overview (Short summary of the functionalities)
    -> Segments for every function set of the plugin
    -> Rules on the server
- Contents of the manual are configurable
    -> Own yml-File

Multiplayer-Tests:
- Are personal configs getting deleted when offline?
- Is the PvPStatus working correctly?
- Are fights getting detected properly?
- Does the vault invite system work?

Polls:
- Whether withers should be able to destroy every block